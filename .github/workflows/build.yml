name: Build & Release EXE

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests pillow pystray pyinstaller

      - name: Build EXE
        run: >
          pyinstaller --onedir --noconsole --strip --name AuEye
          --icon=gold_icon.ico aueye.py
          --collect-all vcruntime
          --exclude-module numpy --exclude-module tkinter.test
          --exclude-module setuptools --exclude-module distutils
          --exclude-module pip --exclude-module pkg_resources

      - name: Package release
        run: Compress-Archive -Path dist\AuEye -DestinationPath dist\AuEye.zip

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/AuEye.zip
          draft: false
          prerelease: false
